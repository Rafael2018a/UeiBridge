<UserControl x:Class="UeiBridgeSetup.Views.EndPointView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UeiBridgeSetup.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding BoxHeader}" TextDecorations="Underline" Margin="5" FontSize="16" FontWeight="Bold"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="IPAddress" Margin="5" FontSize="16"/>
            <TextBox Width="100" Margin="5"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="Port" Margin="5" FontSize="16"/>
            <TextBox Width="50" Margin="5"/>
        </StackPanel>
    </Grid>

</UserControl>
